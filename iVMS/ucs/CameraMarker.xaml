<UserControl x:Class="V3SClient.ucs.CameraMarker"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:V3SClient.ucs"
             mc:Ignorable="d" 
             d:Height="100" d:Width="100">
    <UserControl.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary Source="../styles/StyleCameraView.xaml" />
                <ResourceDictionary Source="../styles/border_reseted.xaml" />
            </ResourceDictionary.MergedDictionaries>
        </ResourceDictionary>
    </UserControl.Resources>

  
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="40" />
            <RowDefinition Height="60" />
        </Grid.RowDefinitions>
        <TextBlock x:Name="txtPerson" Grid.Row="0"
                   Style="{StaticResource General_TextBlock}"
                   Text="{Binding CameraMan.long_Name}"
                   Foreground="Black"
                   FontWeight="Medium"
                   
                   />
        <Border Grid.Row="1">
            <Grid>
                <Image Style="{StaticResource ImageFull}"
                      Source="{Binding ImageFileMarker}"
                       Stretch="Fill"
                      
                       Width="60"
                       Height="60" />
                <Image x:Name="imgPerson"
                       Style="{StaticResource ImageFull}"
                       Stretch="Fill"
                       Source="{Binding ImageFilePerson}"
                       Width="32"
                       Height="32"
                       VerticalAlignment="Top"
                       Margin="0,4,0,0">

                    <Image.Clip>
                        <EllipseGeometry Center="16,16"
                                         RadiusX="8"
                                         RadiusY="8" />
                    </Image.Clip>
                  
                </Image>
            </Grid>

        </Border>

    </Grid>
</UserControl>




















