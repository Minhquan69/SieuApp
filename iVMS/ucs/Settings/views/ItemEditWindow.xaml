<Window x:Class="V3SClient.ucs.Settings.views.ItemEditWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:V3SClient.ucs.Settings.views"
        xmlns:converters="clr-namespace:V3SClient.UI.Converters"
        xmlns:edittemplate="clr-namespace:V3SClient.UI.Selectors"
        xmlns:itemEdit="clr-namespace:V3SClient.libs"
        mc:Ignorable="d"
        Title="{Binding WindowTitle}" Height="Auto" Width="580"
        WindowStartupLocation="CenterScreen" Background="#0D0F12"
        x:Name="RootWindow" 
        WindowStyle="None"
        AllowsTransparency="False" 
        ResizeMode="NoResize">
    <Window.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary Source="../../../styles/customize_button.xaml" />
                <ResourceDictionary Source="../../../styles/customize_image.xaml" />
                <ResourceDictionary Source="../../../styles/customize_textblock.xaml" />
                <ResourceDictionary Source="../../../styles/customize_datagrid.xaml"/>
                <ResourceDictionary Source="../../../styles/border_reseted.xaml" />
                <ResourceDictionary Source="../../../styles/customize_scrollviewer.xaml" />
                <ResourceDictionary Source="../../../styles/customize_combobox.xaml" />
                <ResourceDictionary Source="../../../styles/customize_textbox.xaml" />
                
            </ResourceDictionary.MergedDictionaries>
            <converters:BoolToVisibilityConverter x:Key="BoolToVisibilityConverter"/>
            <DataTemplate x:Key="TextBoxTemplate">
                <TextBox Style="{StaticResource CustomTextBoxStyle}"
                         Height="40" Margin="1" FontSize="14"
                         Loaded="DynamicTextBox_Loaded"/>
            </DataTemplate>

            <DataTemplate x:Key="CheckBoxTemplate">
                <CheckBox Margin="0,10,0,0" Loaded="DynamicCheckBox_Loaded"/>
            </DataTemplate>

            <DataTemplate x:Key="ComboBoxTemplate">
                <ComboBox  Loaded="DynamicComboBox_Loaded" ItemsSource="{Binding ComboItems}" DisplayMemberPath="Value" SelectedValuePath="Key" Style="{StaticResource CustomComboBoxStyle}" />
            </DataTemplate>
            <DataTemplate x:Key="ListBoxMultiSelectTemplate">
                <ListBox ItemsSource="{Binding ComboItems}"  SelectionMode="Multiple" DisplayMemberPath="Value" SelectedValuePath="Key" Height="100"  Background="Transparent" Foreground="White"  BorderBrush="#252830" BorderThickness="1" Loaded="DynamicListBox_Loaded" />
            </DataTemplate>
            <edittemplate:EditFieldTemplateSelector x:Key="EditFieldTemplateSelector"
                      TextBoxTemplate="{StaticResource TextBoxTemplate}"
                      CheckBoxTemplate="{StaticResource CheckBoxTemplate}"
                      ComboBoxTemplate="{StaticResource ComboBoxTemplate}"
                      ListBoxMultiSelectTemplate="{StaticResource ListBoxMultiSelectTemplate}"/>
        </ResourceDictionary>
        
    </Window.Resources>

    <Border BorderBrush="#252830" Background="#111318" BorderThickness="1" CornerRadius="5" Padding="10" MouseLeftButtonDown="Border_MouseLeftButtonDown">
        <Border.Effect>
            <DropShadowEffect Color="LightGray"  Direction="270"  ShadowDepth="5"  Opacity="0.4"  BlurRadius="8"/>
        </Border.Effect>
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="30" />
                <RowDefinition Height="20" />
                <RowDefinition Height="*" />
                <!-- nội dung -->
                <RowDefinition Height="Auto" />
                <!-- Nút -->
            </Grid.RowDefinitions>
            <TextBlock  Text="{Binding WindowTitle}" Style="{StaticResource label_TextBlock }" Margin="0,3,0,0" FontSize="24"/>
            <Separator Grid.Row="1" Margin="5,1,5,1" Background="#252830" DockPanel.Dock="Top" Height="0.8"/>

            <ScrollViewer Grid.Row="2" VerticalScrollBarVisibility="Auto">
                <ScrollViewer.Resources>
                    <ResourceDictionary Source="/styles/settings_base.xaml" />
                </ScrollViewer.Resources>
                <StackPanel Margin="10" x:Name="ContainerPanel">
                    <ItemsControl ItemsSource="{Binding Fields}">
                        <ItemsControl.ItemTemplate>
                            <DataTemplate>
                                <Grid Margin="0,5">
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="150"/>
                                        <!-- Label -->
                                        <ColumnDefinition Width="*"/>
                                        <!-- TextBox / ComboBox -->
                                        <ColumnDefinition Width="Auto"/>
                                        <!-- Button -->
                                    </Grid.ColumnDefinitions>
                                    <TextBlock Text="{Binding Label}" Width="150" Style="{StaticResource label_TextBlock}" />
                                    <ContentPresenter Grid.Column="1" Content="{Binding}" ContentTemplateSelector="{StaticResource EditFieldTemplateSelector}" />
                                    <Button Grid.Column="2" Content="{Binding ButtonLabel}"  Command="{Binding ButtonCommand}"  Visibility="{Binding ShowButton, Converter={StaticResource BoolToVisibilityConverter}}" Height="40" Margin="10,0,0,0" Padding="5,0" FontSize="13" Background="#505082f7" Foreground="White" >
                                        <Button.Template>
                                            <ControlTemplate TargetType="Button">
                                                <Border x:Name="border" Background="{TemplateBinding Background}" CornerRadius="2" BorderBrush="Transparent" BorderThickness="1">
                                                    <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center" Margin="1"/>
                                                </Border>
                                            </ControlTemplate>
                                        </Button.Template>
                                           
                                    </Button>
                                </Grid>
                            </DataTemplate>
                        </ItemsControl.ItemTemplate>
                    </ItemsControl>
                </StackPanel>
            </ScrollViewer>


            <StackPanel Grid.Row="3" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,10,0,0" >
                <!-- Lưu -->
                <Button Width="100" Height="35" Margin="5" Background="#5028a745" Foreground="White" FontWeight="Bold" Cursor="Hand" Command="{Binding SaveCommand}">
                    <Button.Template>
                        <ControlTemplate TargetType="Button">
                            <Border x:Name="border" Background="{TemplateBinding Background}" CornerRadius="6" BorderBrush="Transparent" BorderThickness="1">
                                <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                            </Border>
                            <ControlTemplate.Triggers>
                                <Trigger Property="IsMouseOver" Value="True">
                                    <Setter TargetName="border" Property="Background" Value="#FF3E8E41"/>
                                </Trigger>
                                <Trigger Property="IsPressed" Value="True">
                                    <Setter TargetName="border" Property="Background" Value="#FF2C6A31"/>
                                </Trigger>
                                <Trigger Property="IsEnabled" Value="False">
                                    <Setter TargetName="border" Property="Background" Value="#FFC0C0C0"/>
                                    <Setter Property="Foreground" Value="#FF6D6D6D"/>
                                </Trigger>
                            </ControlTemplate.Triggers>
                        </ControlTemplate>
                    </Button.Template>
                    <StackPanel Orientation="Horizontal" HorizontalAlignment="Center">
                        <TextBlock Text="💾" FontSize="16" Margin="0,0,5,0"/>
                        <TextBlock Text="Lưu" FontSize="14" VerticalAlignment="Center"/>
                    </StackPanel>
                </Button>

                <!-- Đóng -->
                <Button Width="100" Height="35" Margin="5" Background="#50dc3545" Foreground="White" FontWeight="Bold" Cursor="Hand" Command="{Binding CancelCommand}">
                    <Button.Template>
                        <ControlTemplate TargetType="Button">
                            <Border x:Name="border" Background="{TemplateBinding Background}"  CornerRadius="6" BorderBrush="Transparent" BorderThickness="1">
                                <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                            </Border>
                            <ControlTemplate.Triggers>
                                <Trigger Property="IsMouseOver" Value="True">
                                    <Setter TargetName="border" Property="Background" Value="#dc3545"/>
                                </Trigger>
                                <Trigger Property="IsPressed" Value="True">
                                    <Setter TargetName="border" Property="Background" Value="#80dc3545"/>
                                </Trigger>
                                <Trigger Property="IsEnabled" Value="False">
                                    <Setter TargetName="border" Property="Background" Value="#FFC0C0C0"/>
                                    <Setter Property="Foreground" Value="#FF6D6D6D"/>
                                </Trigger>
                            </ControlTemplate.Triggers>
                        </ControlTemplate>
                    </Button.Template>
                    <StackPanel Orientation="Horizontal" HorizontalAlignment="Center">
                        <TextBlock Text="❌" FontSize="16" Margin="0,0,5,0"/>
                        <TextBlock Text="Đóng" FontSize="14" VerticalAlignment="Center"/>
                    </StackPanel>
                </Button>
                

            </StackPanel>
        </Grid>
       
    </Border>
    
</Window>
