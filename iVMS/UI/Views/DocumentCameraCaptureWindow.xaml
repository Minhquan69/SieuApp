<Window x:Class="V3SClient.UI.Views.DocumentCameraCaptureWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:shell="clr-namespace:System.Windows.Shell;assembly=PresentationFramework"
        Title="Xem camera chụp hồ sơ"
        Width="1280"
        Height="820"
        MinWidth="900"
        MinHeight="620"
        WindowStartupLocation="CenterOwner"
        WindowStyle="None"
        ResizeMode="CanResizeWithGrip"
        Background="#0D0F12"
        BorderBrush="#0D0F12"
        BorderThickness="0">
    <shell:WindowChrome.WindowChrome>
        <shell:WindowChrome CaptionHeight="0"
                            CornerRadius="0"
                            GlassFrameThickness="0"
                            ResizeBorderThickness="6"
                            UseAeroCaptionButtons="False" />
    </shell:WindowChrome.WindowChrome>

    <Window.Resources>
        <BooleanToVisibilityConverter x:Key="BoolToVis" />
    </Window.Resources>

    <Grid Background="#0D0F12" SnapsToDevicePixels="True">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <Border Grid.Row="0" Background="#111318" BorderBrush="#252830" BorderThickness="0,0,0,1" Padding="14,10" MouseLeftButtonDown="Header_MouseLeftButtonDown">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="Auto" />
                    <ColumnDefinition Width="Auto" />
                </Grid.ColumnDefinitions>
                <StackPanel>
                    <TextBlock Text="Camera chụp hồ sơ" Foreground="#E8ECF1" FontSize="17" FontWeight="SemiBold" />
                    <TextBlock Text="{Binding DocumentCameraStatus}" Foreground="#8B95A5" FontSize="11" Margin="0,3,0,0" TextTrimming="CharacterEllipsis" />
                </StackPanel>
                <Button Grid.Column="1" Command="{Binding RestartDocumentCameraCommand}" Width="38" Height="34" Margin="0,0,8,0" Background="#161920" BorderBrush="#303744" BorderThickness="1" Foreground="#AAB4C0" FontFamily="Segoe MDL2 Assets" Content="&#xE72C;" ToolTip="Kết nối lại camera" Cursor="Hand" />
                <Button Grid.Column="2" Click="Close_Click" Width="38" Height="34" Background="#161920" BorderBrush="#303744" BorderThickness="1" Foreground="#AAB4C0" FontFamily="Segoe MDL2 Assets" Content="&#xE711;" ToolTip="Đóng" Cursor="Hand" />
            </Grid>
        </Border>

        <Border Grid.Row="1" Background="#05070A" ClipToBounds="True">
            <Grid>
                <Image Source="{Binding LatestDocumentCameraFrame}" Stretch="Uniform" />
                <TextBlock Text="Chưa có tín hiệu camera"
                           Foreground="#667085"
                           FontSize="18"
                           FontWeight="SemiBold"
                           HorizontalAlignment="Center"
                           VerticalAlignment="Center">
                    <TextBlock.Style>
                        <Style TargetType="TextBlock">
                            <Setter Property="Visibility" Value="Collapsed" />
                            <Style.Triggers>
                                <DataTrigger Binding="{Binding LatestDocumentCameraFrame}" Value="{x:Null}">
                                    <Setter Property="Visibility" Value="Visible" />
                                </DataTrigger>
                            </Style.Triggers>
                        </Style>
                    </TextBlock.Style>
                </TextBlock>

                <Button Command="{Binding TakeDocumentPictureCommand}"
                        Width="72"
                        Height="72"
                        BorderThickness="0"
                        BorderBrush="Transparent"
                        Background="#2B5FBF"
                        Foreground="White"
                        Cursor="Hand"
                        HorizontalAlignment="Center"
                        VerticalAlignment="Bottom"
                        Margin="0,0,0,28"
                        ToolTip="Chụp ảnh vào thư mục input">
                    <Button.Template>
                        <ControlTemplate TargetType="Button">
                            <Border Background="{TemplateBinding Background}" BorderBrush="{TemplateBinding BorderBrush}" BorderThickness="{TemplateBinding BorderThickness}" CornerRadius="36">
                                <TextBlock Text="&#xE722;" FontFamily="Segoe MDL2 Assets" FontSize="25" Foreground="White" HorizontalAlignment="Center" VerticalAlignment="Center" />
                            </Border>
                            <ControlTemplate.Triggers>
                                <Trigger Property="IsMouseOver" Value="True">
                                    <Setter Property="Background" Value="#3678E8" />
                                </Trigger>
                                <Trigger Property="IsEnabled" Value="False">
                                    <Setter Property="Background" Value="#475467" />
                                </Trigger>
                            </ControlTemplate.Triggers>
                        </ControlTemplate>
                    </Button.Template>
                </Button>
            </Grid>
        </Border>

        <Border Grid.Row="2" Background="#111318" BorderBrush="#252830" BorderThickness="0,1,0,0" Padding="14,8">
            <Grid>
                <TextBlock Text="{Binding DocumentCameraNotification}" Foreground="#7BA3C9" FontSize="12" TextTrimming="CharacterEllipsis" Visibility="{Binding IsDocumentCameraNotificationVisible, Converter={StaticResource BoolToVis}}" />
                <TextBlock Text="{Binding DocumentRtspUrl}" Foreground="#5A6370" FontSize="11" HorizontalAlignment="Right" TextTrimming="CharacterEllipsis" MaxWidth="560" />
                <ResizeGrip Width="16" Height="16" HorizontalAlignment="Right" VerticalAlignment="Bottom" Margin="0,0,-10,-5" Background="#111318" />
            </Grid>
        </Border>
    </Grid>
</Window>
