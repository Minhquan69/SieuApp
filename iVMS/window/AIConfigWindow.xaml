<Window x:Class="V3SClient.window.AIConfigWindow" xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation" xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml" xmlns:d="http://schemas.microsoft.com/expression/blend/2008" xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" xmlns:local="clr-namespace:V3SClient.window" xmlns:Icon="http://metro.mahapps.com/winfx/xaml/iconpacks" mc:Ignorable="d" ToolTipService.ShowOnDisabled="True" Title="Cấu hình AI" Height="600" Width="600" WindowStyle="None" AllowsTransparency="True" Background="Transparent" WindowStartupLocation="CenterScreen">
    
    <Window.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary Source="../styles/customize_button.xaml" />
                <ResourceDictionary Source="../styles/customize_button_v2.xaml" />
                <ResourceDictionary Source="../styles/customize_textblock.xaml" />
                <ResourceDictionary Source="../styles/customize_checkbox.xaml" />
                <ResourceDictionary Source="../styles/customize_combobox.xaml" />
            <ResourceDictionary Source="/styles/aiconfig_window_styles.xaml" /></ResourceDictionary.MergedDictionaries>

            <BooleanToVisibilityConverter x:Key="BooleanToVisibilityConverter" />
            <local:InverseBooleanToVisibilityConverter x:Key="InverseBooleanToVisibilityConverter" />

            
        </ResourceDictionary>
    </Window.Resources>

    <Border Background="#111318" CornerRadius="8" BorderBrush="#252830" BorderThickness="1">
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="45" />
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
                <RowDefinition Height="*" />
                <RowDefinition Height="55" />
            </Grid.RowDefinitions>

            <!-- Header -->
            <Border Grid.Row="0" Background="#161920" CornerRadius="8,8,0,0" MouseLeftButtonDown="Header_MouseLeftButtonDown">
                <Grid>
                    <StackPanel Orientation="Horizontal" VerticalAlignment="Center" Margin="15,0,0,0">
                        <Icon:PackIconMaterial Kind="Brain" Width="18" Height="18" Foreground="#4A9EFF" VerticalAlignment="Center" Margin="0,0,8,0" />
                        <TextBlock Text="Cấu hình AI" FontSize="16" FontWeight="Bold" Foreground="#4A9EFF" VerticalAlignment="Center" />
                    </StackPanel>
                    <Button Click="BtnClose_Click" Background="Transparent" BorderThickness="0" HorizontalAlignment="Right" VerticalAlignment="Center" Margin="0,0,10,0" Width="30" Height="30" Cursor="Hand">
                        <Icon:PackIconMaterial Kind="Close" Width="14" Height="14" Foreground="#8B95A5" />
                    </Button>
                </Grid>
            </Border>

            <!-- Assignment Section (Top Bar) -->
            <Border Grid.Row="1" Background="#161920" Margin="0,0,0,0" BorderBrush="#252830" BorderThickness="0,0,0,1">
                <Grid Margin="20,15">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*" />
                        <ColumnDefinition Width="Auto" />
                    </Grid.ColumnDefinitions>
                    <StackPanel Grid.Column="0" Orientation="Horizontal">
                        <TextBlock Text="Gán AI Service mới:" VerticalAlignment="Center" Foreground="#E8ECF1" Margin="0,0,15,0" />
                        <ComboBox x:Name="cboAvailableServices" Width="250" Height="32" DisplayMemberPath="Name" SelectedValuePath="Id" Style="{StaticResource CustomComboBoxStyle}" />
                    </StackPanel>
                    <StackPanel Grid.Column="1" Orientation="Horizontal">
                        <Button Content="Gán" Width="80" Height="32" Click="BtnAssign_Click" Margin="10,0,0,0" Background="#4A9EFF" Foreground="#0D0F12" FontWeight="Bold" Cursor="Hand">
                            <Button.Template>
                                <ControlTemplate TargetType="Button">
                                    <Border CornerRadius="4" Background="{TemplateBinding Background}">
                                        <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center" />
                                    </Border>
                                </ControlTemplate>
                            </Button.Template>
                        </Button>
                        <Button Width="32" Height="32" Margin="10,0,0,0" Click="BtnRefresh_Click" Style="{StaticResource toolbarRefreshButton}" ToolTip="Làm mới">
                            <Icon:PackIconMaterial Kind="Refresh" Width="14" Height="14" />
                        </Button>
                    </StackPanel>
                </Grid>
            </Border>

            <TextBlock Grid.Row="2" Text="Danh sách AI đang xử lý" FontWeight="SemiBold" Margin="20,15,20,5" Foreground="#4A9EFF" FontSize="14" />

            <!-- Card List -->
            <ScrollViewer Grid.Row="3" Margin="0,5,0,0" VerticalScrollBarVisibility="Auto">
                <ItemsControl x:Name="icAIAssignments" Margin="20,5">
                    <ItemsControl.ItemTemplate>
                        <DataTemplate>
                            <Border Background="#161920" BorderBrush="#252830" BorderThickness="1" CornerRadius="6" Margin="0,0,0,12" Padding="15">
                                <StackPanel>
                                    <Grid Margin="0,0,0,10">
                                        <Grid.ColumnDefinitions>
                                            <ColumnDefinition Width="*" />
                                            <ColumnDefinition Width="Auto" />
                                        </Grid.ColumnDefinitions>
                                        <StackPanel Orientation="Horizontal" Grid.Column="0">
                                            <Ellipse Width="8" Height="8" Margin="0,0,10,0" Style="{StaticResource AiconfigWindowEllipse01Style}">
                                                
                                            </Ellipse>
                                            <StackPanel VerticalAlignment="Center">
                                                <TextBlock Text="{Binding ServiceName}" Foreground="#E8ECF1" FontWeight="Bold" FontSize="14" />
                                                <StackPanel Orientation="Horizontal" Margin="0,2,0,0">
                                                    <TextBlock Text="Node: " Foreground="#8B95A5" FontSize="11" />
                                                    <TextBlock Text="{Binding NodeName}" Foreground="#8B95A5" FontSize="11" FontWeight="SemiBold" />
                                                </StackPanel>
                                            </StackPanel>
                                        </StackPanel>
                                        <StackPanel Orientation="Horizontal" Grid.Column="1">
                                            <CheckBox Style="{StaticResource AiConfigWindowSwitchStyle}" IsChecked="{Binding IsEnabled, Mode=TwoWay}" Click="OnSettingChanged" Margin="0,0,15,0" VerticalAlignment="Center" />
                                            <Button Click="BtnRemove_Click" Background="Transparent" BorderThickness="0" Cursor="Hand" ToolTip="Gỡ bỏ">
                                                <StackPanel Orientation="Horizontal">
                                                    <Icon:PackIconMaterial Kind="Delete" Width="14" Height="14" Foreground="#FF5252" Margin="0,0,5,0" />
                                                    <TextBlock Text="Gỡ bỏ" Foreground="#FF5252" FontSize="12" VerticalAlignment="Center" />
                                                </StackPanel>
                                            </Button>
                                        </StackPanel>
                                    </Grid>
 
                                    <!-- BodyCam Configuration Panel -->
                                    <Border BorderBrush="#252830" BorderThickness="0,1,0,0" Padding="0,10,0,0" Visibility="{Binding RelativeSource={RelativeSource AncestorType=Window}, Path=IsBodyCam, Converter={StaticResource BooleanToVisibilityConverter}}">
                                        <StackPanel>
                                            <Grid Margin="0,0,0,8">
                                                <Grid.ColumnDefinitions>
                                                    <ColumnDefinition Width="150" />
                                                    <ColumnDefinition Width="*" />
                                                </Grid.ColumnDefinitions>
                                                <StackPanel Orientation="Horizontal">
                                                    <Icon:PackIconMaterial Kind="AccountSettings" Width="14" Height="14" Foreground="#4A9EFF" Margin="0,0,8,0" VerticalAlignment="Center" />
                                                    <TextBlock Text="Vai trò thiết bị:" Foreground="#8B95A5" VerticalAlignment="Center" />
                                                </StackPanel>
                                                <ComboBox Grid.Column="1" SelectedValue="{Binding BodyCam.Role, Mode=TwoWay}" SelectedValuePath="Tag" SelectionChanged="OnSettingChanged" Style="{StaticResource CustomComboBoxStyle}" Height="28" Width="180" HorizontalAlignment="Left">
                                                    <ComboBoxItem Content="Bộ đàm trung tâm" Tag="central_radio" />
                                                    <ComboBoxItem Content="Thiết bị Client" Tag="client_device" />
                                                </ComboBox>
                                            </Grid>
 
                                            <Grid Margin="0,0,0,12">
                                                <Grid.ColumnDefinitions>
                                                    <ColumnDefinition Width="150" />
                                                    <ColumnDefinition Width="*" />
                                                </Grid.ColumnDefinitions>
                                                <StackPanel Orientation="Horizontal">
                                                    <Icon:PackIconMaterial Kind="VolumeHigh" Width="14" Height="14" Foreground="#4A9EFF" Margin="0,0,8,0" VerticalAlignment="Center" />
                                                    <TextBlock Text="Chế độ phản hồi:" Foreground="#8B95A5" VerticalAlignment="Center" />
                                                </StackPanel>
                                                <ComboBox Grid.Column="1" SelectedValue="{Binding BodyCam.FeedbackMode, Mode=TwoWay}" SelectedValuePath="Tag" SelectionChanged="OnSettingChanged" Style="{StaticResource CustomComboBoxStyle}" Height="28" Width="180" HorizontalAlignment="Left" Margin="0,0,0,5">
                                                    <ComboBoxItem Content="Không có" Tag="none" />
                                                    <ComboBoxItem Content="Rung" Tag="vibrate" />
                                                    <ComboBoxItem Content="Tiếng bíp" Tag="beep" />
                                                    <ComboBoxItem Content="Rung &amp; Bíp" Tag="vibrate_beep" />
                                                    <ComboBoxItem Content="Giọng nói" Tag="voice" />
                                                </ComboBox>
                                            </Grid>

                                            <StackPanel Orientation="Horizontal">
                                                <StackPanel Orientation="Horizontal" Margin="0,0,25,0">
                                                    <Icon:PackIconMaterial Kind="Video" Width="14" Height="14" Foreground="#8B95A5" Margin="0,0,5,0" VerticalAlignment="Center" />
                                                    <TextBlock Text="Media:" Foreground="#8B95A5" VerticalAlignment="Center" Margin="0,0,8,0" />
                                                    <CheckBox Style="{StaticResource AiConfigWindowSwitchStyle}" IsChecked="{Binding BodyCam.Streams.Media, Mode=TwoWay}" Click="OnSettingChanged">
                                                        <CheckBox.LayoutTransform>
                                                            <ScaleTransform ScaleX="0.8" ScaleY="0.8" />
                                                        </CheckBox.LayoutTransform>
                                                    </CheckBox>
                                                </StackPanel>
                                                <StackPanel Orientation="Horizontal" Margin="0,0,25,0">
                                                    <Icon:PackIconMaterial Kind="Microphone" Width="14" Height="14" Foreground="#8B95A5" Margin="0,0,5,0" VerticalAlignment="Center" />
                                                    <TextBlock Text="Talk:" Foreground="#8B95A5" VerticalAlignment="Center" Margin="0,0,8,0" />
                                                    <CheckBox Style="{StaticResource AiConfigWindowSwitchStyle}" IsChecked="{Binding BodyCam.Streams.Talk, Mode=TwoWay}" Click="OnSettingChanged">
                                                        <CheckBox.LayoutTransform>
                                                            <ScaleTransform ScaleX="0.8" ScaleY="0.8" />
                                                        </CheckBox.LayoutTransform>
                                                    </CheckBox>
                                                </StackPanel>
                                                <StackPanel Orientation="Horizontal">
                                                    <Icon:PackIconMaterial Kind="Earth" Width="14" Height="14" Foreground="#8B95A5" Margin="0,0,5,0" VerticalAlignment="Center" />
                                                    <TextBlock Text="GPS:" Foreground="#8B95A5" VerticalAlignment="Center" Margin="0,0,8,0" />
                                                    <CheckBox Style="{StaticResource AiConfigWindowSwitchStyle}" IsChecked="{Binding BodyCam.Streams.Gps, Mode=TwoWay}" Click="OnSettingChanged">
                                                        <CheckBox.LayoutTransform>
                                                            <ScaleTransform ScaleX="0.8" ScaleY="0.8" />
                                                        </CheckBox.LayoutTransform>
                                                    </CheckBox>
                                                </StackPanel>
                                            </StackPanel>
                                        </StackPanel>
                                    </Border>

                                    <!-- Placeholder for non-BodyCam -->
                                    <TextBlock Text="Cấu hình tham số AI chuẩn cho IP Camera..." Foreground="#5C5C5C" FontSize="11" FontStyle="Italic" Visibility="{Binding RelativeSource={RelativeSource AncestorType=Window}, Path=IsBodyCam, Converter={StaticResource InverseBooleanToVisibilityConverter}}" Margin="0,5,0,0" />
                                </StackPanel>
                            </Border>
                        </DataTemplate>
                    </ItemsControl.ItemTemplate>
                </ItemsControl>
            </ScrollViewer>

            <!-- Footer -->
            <Border Grid.Row="4" Background="#161920" CornerRadius="0,0,8,8" BorderBrush="#252830" BorderThickness="0,1,0,0">
                <StackPanel Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,0,15,0" VerticalAlignment="Center">
                    <Button Content="Đóng" Click="BtnClose_Click" Width="90" Height="32" Cursor="Hand">
                        <Button.Template>
                            <ControlTemplate TargetType="Button">
                                <Border CornerRadius="4" Background="#252830" Padding="8,4">
                                    <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center" />
                                </Border>
                            </ControlTemplate>
                        </Button.Template>
                    </Button>
                </StackPanel>
            </Border>
        </Grid>
    </Border>
</Window>




















