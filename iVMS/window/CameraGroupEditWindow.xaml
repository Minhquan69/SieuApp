<Window x:Class="V3SClient.window.CameraGroupEditWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:Icon="http://metro.mahapps.com/winfx/xaml/iconpacks"
        Title="Chỉnh sửa Nhóm camera" Height="500" Width="450"
        WindowStartupLocation="CenterScreen" ResizeMode="NoResize"
        WindowStyle="None" AllowsTransparency="True" Background="Transparent"
        FontFamily="Arial">
    <Window.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary Source="../styles/customize_combobox.xaml" />
                <ResourceDictionary Source="../styles/customize_textbox.xaml" />
                <ResourceDictionary Source="../styles/customize_scrollviewer.xaml" />
                <ResourceDictionary Source="../styles/customize_button.xaml" />
                <ResourceDictionary Source="/styles/form_base.xaml" />
            </ResourceDictionary.MergedDictionaries>
        </ResourceDictionary>
    </Window.Resources>

    <Border Background="#111318" CornerRadius="8" BorderBrush="#252830" BorderThickness="1">
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="45"/>
                <RowDefinition Height="*"/>
                <RowDefinition Height="55"/>
            </Grid.RowDefinitions>

            <!-- Header -->
            <Border Grid.Row="0" Background="#161920" CornerRadius="8,8,0,0" MouseLeftButtonDown="Header_MouseLeftButtonDown">
                <Grid>
                    <StackPanel Orientation="Horizontal" VerticalAlignment="Center" Margin="15,0,0,0">
                        <Icon:PackIconMaterial Kind="FolderNetwork" Width="18" Height="18" Foreground="#4A9EFF" VerticalAlignment="Center" Margin="0,0,8,0"/>
                        <TextBlock x:Name="txtTitle" Text="Thêm Nhóm camera mới" FontSize="16" FontWeight="Bold" Foreground="#E8ECF1" VerticalAlignment="Center"/>
                    </StackPanel>
                    <Button Click="BtnCancel_Click" Background="Transparent" BorderThickness="0" HorizontalAlignment="Right" VerticalAlignment="Center" Margin="0,0,10,0" Width="30" Height="30" Cursor="Hand">
                        <Icon:PackIconMaterial Kind="Close" Width="14" Height="14" Foreground="#8B95A5"/>
                    </Button>
                </Grid>
            </Border>

            <!-- Form Fields -->
            <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto" Style="{StaticResource CustomScrollViewerStyle}">
                <StackPanel Margin="20,10,20,10">
                    <TextBlock Text="Tên nhóm *" Style="{StaticResource FieldLabelBase}"/>
                    <TextBox x:Name="txtName" Style="{StaticResource FieldInputBase}" Tag="Nhập tên nhóm"/>

                    <TextBlock Text="Mã nhóm" Style="{StaticResource FieldLabelBase}"/>
                    <TextBox x:Name="txtCode" Style="{StaticResource FieldInputBase}" Tag="Nhập mã nhóm"/>

                    <TextBlock Text="Phân loại nhóm *" Style="{StaticResource FieldLabelBase}"/>
                    <ComboBox x:Name="cboType" Style="{StaticResource FieldComboBase}" SelectionChanged="CboType_SelectionChanged">
                        <ComboBoxItem Content="Địa lý (Quận, Phường, Tòa nhà)" Tag="geographical" IsSelected="True"/>
                        <ComboBoxItem Content="Chức năng (Phân loại cam)" Tag="functional"/>
                        <ComboBoxItem Content="Nhóm đàm thoại (Bộ đàm)" Tag="talk_group"/>
                    </ComboBox>

                    <StackPanel x:Name="panelTalkId" Visibility="Collapsed">
                        <TextBlock Text="Kênh đàm thoại (Talk ID) *" Style="{StaticResource FieldLabelBase}"/>
                        <TextBox x:Name="txtTalkId" Style="{StaticResource FieldInputBase}" PreviewTextInput="NumberValidationTextBox" Tag="Nhập Talk ID"/>
                    </StackPanel>

                    <TextBlock Text="Nhóm cha (Cấp trên)" Style="{StaticResource FieldLabelBase}"/>
                    <ComboBox x:Name="cboParent" Style="{StaticResource FieldComboBase}" DisplayMemberPath="Name" SelectedValuePath="Id"/>

                    <TextBlock Text="Mô tả" Style="{StaticResource FieldLabelBase}"/>
                    <TextBox x:Name="txtDescription" Style="{StaticResource FieldInputBase}" TextWrapping="Wrap" AcceptsReturn="True" Height="80" Tag="Nhập mô tả"/>
                </StackPanel>
            </ScrollViewer>

            <!-- Footer -->
            <Border Grid.Row="2" Background="#161920" CornerRadius="0,0,8,8">
                <StackPanel Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,0,15,0" VerticalAlignment="Center">
                    <Button Content="Hủy" Click="BtnCancel_Click" Width="80" Height="32" Margin="0,0,10,0" Cursor="Hand">
                        <Button.Template>
                            <ControlTemplate TargetType="Button">
                                <Border CornerRadius="4" Background="#252830" Padding="8,4">
                                    <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                </Border>
                            </ControlTemplate>
                        </Button.Template>
                    </Button>
                    <Button x:Name="btnSave" Content="Lưu" Click="BtnSave_Click" Width="100" Height="32" Background="#4A9EFF" Foreground="#0D0F12" FontWeight="Bold" Cursor="Hand">
                        <Button.Template>
                            <ControlTemplate TargetType="Button">
                                <Border CornerRadius="4" Background="{TemplateBinding Background}">
                                    <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                </Border>
                            </ControlTemplate>
                        </Button.Template>
                    </Button>
                </StackPanel>
            </Border>
        </Grid>
    </Border>
</Window>


















