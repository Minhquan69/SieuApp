<?xml version="1.0" encoding="utf-8"?>
<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation" xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
  <!-- ScrollBar Style -->
  <Style x:Key="MyScrollBar" TargetType="ScrollBar">
    <Setter Property="Width" Value="5" />
    <Setter Property="Background" Value="#0F1117" />
    <Setter Property="Template">
      <Setter.Value>
        <ControlTemplate TargetType="ScrollBar">
          <Grid>
            <Rectangle Fill="{TemplateBinding Background}" RadiusX="2" RadiusY="2" />
            <Track Name="PART_Track" IsDirectionReversed="true">
              <!--<Track.DecreaseRepeatButton>
                                <RepeatButton Command="ScrollBar.LineUpCommand"
                                              Width="{TemplateBinding Width}" 
                                              Background="Transparent"/>
                            </Track.DecreaseRepeatButton>-->
              <Track.Thumb>
                <Thumb Height="auto" Background="#303848" />
              </Track.Thumb>
              <!--<Track.IncreaseRepeatButton>
                                <RepeatButton Command="ScrollBar.LineDownCommand"
                                              Width="{TemplateBinding Width}"
                                              Background="Transparent"/>
                            </Track.IncreaseRepeatButton>-->
            </Track>
          </Grid>
        </ControlTemplate>
      </Setter.Value>
    </Setter>
  </Style>
  <!-- ScrollViewer Style -->
  <Style TargetType="ScrollViewer" x:Key="CustomScrollViewerStyle">
    <Setter Property="Template">
      <Setter.Value>
        <ControlTemplate TargetType="ScrollViewer">
          <Grid>
            <Grid.ColumnDefinitions>
              <ColumnDefinition Width="*" />
              <ColumnDefinition Width="5" />
            </Grid.ColumnDefinitions>
            <Grid.RowDefinitions>
              <RowDefinition Height="*" />
              <RowDefinition Height="5" />
            </Grid.RowDefinitions>
            <!-- N?i dung chính -->
            <ScrollContentPresenter x:Name="PART_ScrollContentPresenter" Grid.Row="0" Grid.Column="0" Content="{TemplateBinding Content}" ContentTemplate="{TemplateBinding ContentTemplate}" CanContentScroll="{TemplateBinding CanContentScroll}" />
            <!-- Thanh cu?n d?c -->
            <ScrollBar x:Name="PART_VerticalScrollBar" Grid.Row="0" Grid.Column="1" Orientation="Vertical" Visibility="{TemplateBinding ComputedVerticalScrollBarVisibility}" Maximum="{TemplateBinding ScrollableHeight}" ViewportSize="{TemplateBinding ViewportHeight}" Value="{TemplateBinding VerticalOffset}" Style="{StaticResource MyScrollBar}" SmallChange="5" LargeChange="10" />
            <!-- Thanh cu?n ngang -->
            <ScrollBar x:Name="PART_HorizontalScrollBar" Grid.Row="1" Grid.Column="0" Orientation="Horizontal" Visibility="{TemplateBinding ComputedHorizontalScrollBarVisibility}" Maximum="{TemplateBinding ScrollableWidth}" ViewportSize="{TemplateBinding ViewportWidth}" Value="{TemplateBinding HorizontalOffset}" Style="{StaticResource MyScrollBar}" SmallChange="5" LargeChange="10" />
          </Grid>
        </ControlTemplate>
      </Setter.Value>
    </Setter>
  </Style>
</ResourceDictionary>