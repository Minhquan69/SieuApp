<?xml version="1.0" encoding="utf-8"?>
<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation" xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml" xmlns:d="http://schemas.microsoft.com/expression/blend/2008" xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006">
  <ResourceDictionary.MergedDictionaries>
    <ResourceDictionary Source="/styles/border_reseted.xaml" />
  </ResourceDictionary.MergedDictionaries>
  <Style x:Key="SmartDownloadWindowLabelStyle" TargetType="TextBlock" BasedOn="{StaticResource General_TextBlock}">
    <Setter Property="FontSize" Value="12" />
    <Setter Property="Foreground" Value="#8B95A5" />
    <Setter Property="Margin" Value="0,10,0,5" />
  </Style>
  <Style x:Key="SmartDownloadWindowInputTextBoxStyle" TargetType="TextBox">
    <Setter Property="Background" Value="#161920" />
    <Setter Property="Foreground" Value="#E8ECF1" />
    <Setter Property="BorderBrush" Value="#252830" />
    <Setter Property="Padding" Value="8,5" />
    <Setter Property="FontSize" Value="13" />
    <Setter Property="VerticalContentAlignment" Value="Center" />
    <Setter Property="BorderThickness" Value="1" />
  </Style>
  <Style x:Key="SmartDownloadWindowPrimaryButtonStyle" TargetType="Button">
    <Setter Property="Background" Value="#4A9EFF" />
    <Setter Property="Foreground" Value="White" />
    <Setter Property="Padding" Value="20,8" />
    <Setter Property="BorderThickness" Value="0" />
    <Setter Property="FontSize" Value="13" />
    <Setter Property="FontWeight" Value="SemiBold" />
    <Style.Triggers>
      <Trigger Property="IsMouseOver" Value="True">
        <Setter Property="Background" Value="#3B85E0" />
      </Trigger>
      <Trigger Property="IsEnabled" Value="False">
        <Setter Property="Background" Value="#252830" />
        <Setter Property="Foreground" Value="#8B95A5" />
      </Trigger>
    </Style.Triggers>
  </Style>
  <Style x:Key="SmartDownloadWindowSecondaryButtonStyle" TargetType="Button">
    <Setter Property="Background" Value="#252830" />
    <Setter Property="Foreground" Value="#E8ECF1" />
    <Setter Property="Padding" Value="20,8" />
    <Setter Property="BorderThickness" Value="0" />
    <Setter Property="FontSize" Value="13" />
    <Style.Triggers>
      <Trigger Property="IsMouseOver" Value="True">
        <Setter Property="Background" Value="#303540" />
      </Trigger>
    </Style.Triggers>
  </Style>
</ResourceDictionary>