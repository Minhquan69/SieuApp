<UserControl x:Class="V3SClient.ucs.Settings.views.SystemConfigView" xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation" xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml" xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" xmlns:d="http://schemas.microsoft.com/expression/blend/2008" xmlns:Icon="http://metro.mahapps.com/winfx/xaml/iconpacks" xmlns:System="clr-namespace:System;assembly=mscorlib" xmlns:converters="clr-namespace:V3SClient.UI.Converters" xmlns:ucs="clr-namespace:V3SClient.ucs" mc:Ignorable="d" d:DesignHeight="600" d:DesignWidth="800">
    <UserControl.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary Source="../../../styles/customize_button_v2.xaml" />
                <ResourceDictionary Source="../../../styles/customize_textbox.xaml" />
                <ResourceDictionary Source="../../../styles/customize_textblock.xaml" />
                <ResourceDictionary Source="../../../styles/customize_combobox.xaml" />
                <ResourceDictionary Source="../../../styles/customize_scrollviewer.xaml" />
                <ResourceDictionary Source="../../../styles/customize_datagrid_ivista.xaml" />
            <ResourceDictionary Source="/styles/system_config_view_styles.xaml" /></ResourceDictionary.MergedDictionaries>
            <converters:PermissionVisibilityConverter x:Key="PermissionVisConverter" />
        </ResourceDictionary>
    </UserControl.Resources>

    <Border CornerRadius="15,2,2,2" Background="#111318" Margin="12,0,10,0">
        <DockPanel Margin="20" LastChildFill="True">
            <!-- Header -->
            <StackPanel DockPanel.Dock="Top" Margin="0,0,0,20">
                <StackPanel Orientation="Horizontal">
                    <Icon:PackIconMaterial Kind="Cog" Width="24" Height="24" Foreground="#4A9EFF" VerticalAlignment="Center" Margin="0,0,10,0" />
                    <TextBlock Text="Cấu hình hệ thống" FontWeight="Bold" FontSize="22" Foreground="#4A9EFF" VerticalAlignment="Center" />
                </StackPanel>
                <Separator Background="#252830" Margin="0,10,0,0" />
            </StackPanel>

            <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto" Style="{StaticResource CustomScrollViewerStyle}">
                <StackPanel Margin="0,0,20,0">
                    <!-- Section: Server Connection -->
                    <TextBlock Text="Cấu hình Kết nối Server" Foreground="White" FontSize="16" FontWeight="SemiBold" Margin="0,0,0,15" />
                    <Grid Margin="10,0,0,20">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="Auto" /> <!-- Label URL -->
                            <ColumnDefinition Width="*" />    <!-- TextBox URL -->
                            <ColumnDefinition Width="Auto" /> <!-- Label Mode -->
                            <ColumnDefinition Width="Auto" /> <!-- ComboBox Mode -->
                        </Grid.ColumnDefinitions>
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto" />
                        </Grid.RowDefinitions>

                        <!-- Center Manager URL -->
                        <StackPanel Grid.Column="0" VerticalAlignment="Center" Margin="0,0,15,0">
                            <TextBlock Text="Center Manager URL" Foreground="#CCC" FontSize="13" />
                            <TextBlock Text="Địa chỉ chính của hệ thống" Foreground="#666" FontSize="10" FontStyle="Italic" />
                        </StackPanel>
                        <TextBox Grid.Column="1" Text="{Binding ApiUrl, UpdateSourceTrigger=PropertyChanged}" Style="{StaticResource CustomTextBoxStyle}" Height="30" VerticalContentAlignment="Center" Margin="0,0,30,0" />

                        <!-- Network Mode -->
                        <TextBlock Grid.Column="2" Text="Chế độ mạng" Foreground="#CCC" FontSize="13" VerticalAlignment="Center" Margin="0,0,15,0" />
                        <ComboBox Grid.Column="3" SelectedValue="{Binding NetworkMode}" Width="110" Height="30" Style="{StaticResource CustomComboBoxStyle}" ItemContainerStyle="{StaticResource CustomComboBoxItemStyle}" HorizontalAlignment="Right">
                             <System:String>Public</System:String>
                             <System:String>Internal</System:String>
                        </ComboBox>
                    </Grid>

                    <Separator Background="#252830" Margin="0,0,0,20" />

                    <!-- Section: Endpoints -->
                    <Grid Margin="0,0,0,15">
                        <TextBlock Text="Danh sách Endpoint &amp; Token" Foreground="White" FontSize="16" FontWeight="SemiBold" VerticalAlignment="Center" />
                        <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                            <TextBlock Text="Lần đồng bộ: " Foreground="#ccffffff" FontSize="11" VerticalAlignment="Center" Margin="0,0,5,0" />
                            <TextBlock Text="{Binding LastSyncTime}" Foreground="#ffffff" FontSize="11" FontWeight="Bold" VerticalAlignment="Center" Margin="0,0,20,0" />
                            <Button Command="{Binding SyncCommand}" Style="{StaticResource toolbarAddButton}" Width="120" Height="28">
                                <StackPanel Orientation="Horizontal">
                                    <Icon:PackIconMaterial Kind="Sync" Width="12" Height="12" Spin="{Binding IsSyncing}" Margin="0,0,8,0" VerticalAlignment="Center" />
                                    <TextBlock Text="Đồng bộ ngay" VerticalAlignment="Center" />
                                </StackPanel>
                            </Button>
                        </StackPanel>
                    </Grid>

                    <Border Background="#161920" CornerRadius="8" Padding="1" Margin="0,0,0,20" BorderThickness="1" BorderBrush="#252830">
                        <DataGrid ItemsSource="{Binding Endpoints}" Style="{StaticResource IvistaDataGridStyle}" MaxHeight="300" AutoGenerateColumns="False">
                            <DataGrid.Columns>
                                <DataGridTextColumn Header="Keyword" Binding="{Binding Keyword}" Width="Auto" IsReadOnly="True" />
                                <DataGridTextColumn Header="Tên dịch vụ" Binding="{Binding Name}" Width="160" IsReadOnly="True" />
                                <DataGridTemplateColumn Header="URL" Width="*">
                                    <DataGridTemplateColumn.CellTemplate>
                                        <DataTemplate>
                                            <TextBlock x:Name="txtUrl" Text="{Binding PublicUrl}" VerticalAlignment="Center" Foreground="#E8ECF1" />
                                            <DataTemplate.Triggers>
                                                <DataTrigger Binding="{Binding DataContext.NetworkMode, RelativeSource={RelativeSource AncestorType=UserControl}}" Value="Internal">
                                                    <Setter TargetName="txtUrl" Property="Text" Value="{Binding InternalUrl}" />
                                                </DataTrigger>
                                            </DataTemplate.Triggers>
                                        </DataTemplate>
                                    </DataGridTemplateColumn.CellTemplate>
                                </DataGridTemplateColumn>
                                <DataGridTemplateColumn Header="Trạng thái" Width="110">
                                    <DataGridTemplateColumn.CellTemplate>
                                        <DataTemplate>
                                            <StackPanel Orientation="Horizontal" VerticalAlignment="Center" Margin="5,0,0,0">
                                                <Ellipse Width="8" Height="8" Margin="0,0,8,0" Style="{StaticResource SystemConfigViewEllipse01Style}">
                                                    
                                                </Ellipse>
                                                <TextBlock Text="{Binding Status}" Foreground="#E8ECF1" FontSize="12" VerticalAlignment="Center" />
                                            </StackPanel>
                                        </DataTemplate>
                                    </DataGridTemplateColumn.CellTemplate>
                                </DataGridTemplateColumn>
                            </DataGrid.Columns>
                        </DataGrid>
                    </Border>

                    <Separator Background="#252830" Margin="0,0,0,20" />

                    <!-- Section: MetaAI Optimization -->
                    <TextBlock Text="Optimization" Foreground="White" FontSize="16" FontWeight="SemiBold" Margin="0,0,0,15" />

                    <Grid Margin="10,0,0,20">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="250" />
                            <ColumnDefinition Width="*" />
                        </Grid.ColumnDefinitions>
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto" />
                            <RowDefinition Height="Auto" />
                        </Grid.RowDefinitions>

                        <StackPanel Grid.Row="0" Grid.Column="0" VerticalAlignment="Center" Margin="0,0,0,15">
                            <TextBlock Text="Thời gian load dữ liệu AI (phút)" Foreground="#CCC" FontSize="14" />
                            <TextBlock Text="Giới hạn dữ liệu cũ khi khởi động app" Foreground="#666" FontSize="11" FontStyle="Italic" />
                        </StackPanel>
                        <TextBox Grid.Row="0" Grid.Column="1" Text="{Binding MaxLoadMinutes, UpdateSourceTrigger=PropertyChanged}" Style="{StaticResource CustomTextBoxStyle}" Width="150" Height="30" HorizontalAlignment="Left" Margin="0,0,0,15" />

                        <StackPanel Grid.Row="1" Grid.Column="0" VerticalAlignment="Center">
                            <TextBlock Text="Số lượng bản ghi tối đa " Foreground="#CCC" FontSize="14" />
                            <TextBlock Text="Giới hạn để tránh overload khi dữ liệu lớn" Foreground="#666" FontSize="11" FontStyle="Italic" />
                        </StackPanel>
                        <TextBox Grid.Row="1" Grid.Column="1" Text="{Binding MaxItemsInMemory, UpdateSourceTrigger=PropertyChanged}" Style="{StaticResource CustomTextBoxStyle}" Width="150" Height="30" HorizontalAlignment="Left" />
                    </Grid>

                    <Separator Background="#252830" Margin="0,0,0,20" />

                    <!-- Section: Metadata Filtering -->
                    <TextBlock Text="Filter Metadata (Live)" Foreground="White" FontSize="16" FontWeight="SemiBold" Margin="0,0,0,15" />

                    <Grid Margin="10,0,0,20">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="250" />
                            <ColumnDefinition Width="*" />
                        </Grid.ColumnDefinitions>
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto" />
                            <RowDefinition Height="Auto" />
                            <RowDefinition Height="Auto" />
                            <RowDefinition Height="Auto" />
                            <RowDefinition Height="Auto" />
                        </Grid.RowDefinitions>

                        <!-- Classes -->
                        <StackPanel Grid.Row="0" Grid.Column="0" VerticalAlignment="Center" Margin="0,0,0,15">
                            <TextBlock Text="Danh sách loại đối tượng" Foreground="#CCC" FontSize="14" />
                            <TextBlock Text="Cách nhau bởi dấu phẩy (vd: person, car, face)" Foreground="#666" FontSize="11" FontStyle="Italic" />
                        </StackPanel>
                        <TextBox Grid.Row="0" Grid.Column="1" Text="{Binding AllowedClassesString, UpdateSourceTrigger=PropertyChanged}" Style="{StaticResource CustomTextBoxStyle}" Height="30" HorizontalAlignment="Stretch" Margin="0,0,50,15" />

                        <!-- Classes for Images -->
                        <StackPanel Grid.Row="1" Grid.Column="0" VerticalAlignment="Center" Margin="0,0,0,15">
                            <TextBlock Text="Danh sách object hiển thị ảnh UI Log" Foreground="#CCC" FontSize="14" />
                            <TextBlock Text="Cách nhau bởi dấu phẩy (vd: plate, face)" Foreground="#666" FontSize="11" FontStyle="Italic" />
                        </StackPanel>
                        <TextBox Grid.Row="1" Grid.Column="1" Text="{Binding AllowedImageClassesString, UpdateSourceTrigger=PropertyChanged}" Style="{StaticResource CustomTextBoxStyle}" Height="30" HorizontalAlignment="Stretch" Margin="0,0,50,15" />

                        <!-- Events -->
                        <StackPanel Grid.Row="2" Grid.Column="0" VerticalAlignment="Center" Margin="0,0,0,15">
                            <TextBlock Text="Loại sự kiện hiển thị" Foreground="#CCC" FontSize="14" />
                        </StackPanel>
                        <StackPanel Grid.Row="2" Grid.Column="1" Orientation="Horizontal" VerticalAlignment="Center" Margin="0,0,0,15">
                            <CheckBox Content="Tồn tại " IsChecked="{Binding IsExistSelected}" Foreground="White" Margin="0,0,20,0" />
                            <CheckBox Content="Xuất hiện mới" IsChecked="{Binding IsAppearSelected}" Foreground="White" Margin="0,0,20,0" />
                            <CheckBox Content="Cập nhật " IsChecked="{Binding IsUpdateSelected}" Foreground="White" Margin="0,0,20,0" />
                            <CheckBox Content="Biến mất " IsChecked="{Binding IsDisappearSelected}" Foreground="White" />
                        </StackPanel>

                        <!-- Min Confidence -->
                        <StackPanel Grid.Row="3" Grid.Column="0" VerticalAlignment="Center" Margin="0,0,0,15">
                            <TextBlock Text="Độ tin cậy tối thiểu " Foreground="#CCC" FontSize="14" />
                            <TextBlock Text="Dao động từ 0.1 đến 1.0 (vd: 0.4)" Foreground="#666" FontSize="11" FontStyle="Italic" />
                        </StackPanel>
                        <StackPanel Grid.Row="3" Grid.Column="1" Orientation="Horizontal" Margin="0,0,0,15">
                            <Slider Value="{Binding MinConfidence}" Minimum="0.1" Maximum="1.0" Width="200" VerticalAlignment="Center" Margin="0,0,10,0" />
                            <TextBlock Text="{Binding MinConfidence, StringFormat=N2}" Foreground="White" VerticalAlignment="Center" />
                        </StackPanel>

                        <!-- Roi Info Show -->
                        <StackPanel Grid.Row="4" Grid.Column="0" VerticalAlignment="Center">
                            <TextBlock Text="Hiển thị thời gian obj (ROI)" Foreground="#CCC" FontSize="14" />
                        </StackPanel>
                        <StackPanel Grid.Row="4" Grid.Column="1" Orientation="Horizontal" VerticalAlignment="Center">
                            <CheckBox Content="Hiển thị" IsChecked="{Binding RoiInfoShow}" Foreground="White" />
                        </StackPanel>
                    </Grid>

                    <Separator Background="#252830" Margin="0,0,0,20" />

                    <!-- Save Button -->
                    <Button Command="{Binding SaveCommand}" Style="{StaticResource toolbarAddButton}" Width="120" Height="35" HorizontalAlignment="Left">
                        <StackPanel Orientation="Horizontal">
                            <Icon:PackIconMaterial Kind="ContentSave" Width="16" Height="16" VerticalAlignment="Center" Margin="0,0,8,0" />
                            <TextBlock Text="Lưu cài đặt" VerticalAlignment="Center" FontSize="14" />
                        </StackPanel>
                    </Button>

                </StackPanel>
            </ScrollViewer>
        </DockPanel>
    </Border>
</UserControl>




















