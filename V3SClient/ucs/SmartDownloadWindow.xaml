<Window x:Class="V3SClient.ucs.SmartDownloadWindow" xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation" xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml" xmlns:d="http://schemas.microsoft.com/expression/blend/2008" xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" mc:Ignorable="d" Title="Smart Download" Height="450" Width="400" WindowStyle="None" AllowsTransparency="True" Background="Transparent" WindowStartupLocation="CenterOwner">
    <Window.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary Source="../styles/border_reseted.xaml" />
            <ResourceDictionary Source="/styles/smart_download_window_styles.xaml" /></ResourceDictionary.MergedDictionaries>
            
            
            
            

            

            
        </ResourceDictionary>
    </Window.Resources>

    <Border Background="#0D0F12" CornerRadius="8" BorderBrush="#252830" BorderThickness="1">
        <Grid Margin="15">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto" /> <!-- Header -->
                <RowDefinition Height="*" />    <!-- Content -->
                <RowDefinition Height="Auto" /> <!-- Progress -->
                <RowDefinition Height="Auto" /> <!-- Footer -->
            </Grid.RowDefinitions>

            <!-- Header -->
            <Grid Grid.Row="0" Margin="0,0,0,15">
                <Border Background="Transparent" MouseLeftButtonDown="Header_MouseLeftButtonDown">
                    <TextBlock Text="TẢI XUỐNG VIDEO" Foreground="#4A9EFF" FontSize="16" FontWeight="Bold" VerticalAlignment="Center" />
                </Border>
                <Button HorizontalAlignment="Right" VerticalAlignment="Center" Click="Close_Click" Background="Transparent" BorderThickness="0" Cursor="Hand">
                    <TextBlock Text="" FontFamily="Segoe MDL2 Assets" Foreground="#8B95A5" FontSize="14" />
                </Button>
            </Grid>

            <!-- Content -->
            <StackPanel Grid.Row="1">
                <TextBlock x:Name="txtCameraCount" Text="0 thiết bị được chọn" Foreground="#4A9EFF" FontSize="13" FontWeight="SemiBold" />
                
                <TextBlock Text="THỜI GIAN BẮT ĐẦU (YYYY-MM-DD HH:MM:SS)" Style="{StaticResource SmartDownloadWindowLabelStyle}" />
                <TextBox x:Name="txtStartTime" Style="{StaticResource SmartDownloadWindowInputTextBoxStyle}" />

                <TextBlock Text="THỜI GIAN KẾT THÚC (YYYY-MM-DD HH:MM:SS)" Style="{StaticResource SmartDownloadWindowLabelStyle}" />
                <TextBox x:Name="txtEndTime" Style="{StaticResource SmartDownloadWindowInputTextBoxStyle}" />

                <TextBlock Text="TẢI VỀ THƯ MỤC" Style="{StaticResource SmartDownloadWindowLabelStyle}" />
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*" />
                        <ColumnDefinition Width="Auto" />
                    </Grid.ColumnDefinitions>
                    <TextBox x:Name="txtSavePath" Style="{StaticResource SmartDownloadWindowInputTextBoxStyle}" IsReadOnly="True" Margin="0,0,5,0" />
                    <Button Grid.Column="1" Content="Chọn..." Style="{StaticResource SmartDownloadWindowSecondaryButtonStyle}" Click="Browse_Click" Padding="10,5" />
                </Grid>

                <CheckBox x:Name="chkMetadata" Content="Bao gồm dữ liệu AI (.json)" Foreground="#E8ECF1" Margin="0,20,0,0" IsChecked="True" FontSize="13" />
            </StackPanel>

            <!-- Progress -->
            <StackPanel x:Name="pnlProgress" Grid.Row="2" Margin="0,20,0,0" Visibility="Collapsed">
                <Grid Margin="0,0,0,5">
                    <TextBlock Text="Tiến trình tổng thể" Foreground="#8B95A5" FontSize="12" />
                    <TextBlock x:Name="txtPercent" Text="0%" Foreground="#8B95A5" FontSize="12" HorizontalAlignment="Right" />
                </Grid>
                <ProgressBar x:Name="pbOverall" Height="6" Background="#161920" Foreground="#4A9EFF" BorderThickness="0" />
                <TextBlock x:Name="txtStatus" Text="Đang khởi tạo..." Padding="0,5,0,0" Foreground="#8B95A5" FontSize="11" TextAlignment="Center" />
            </StackPanel>

            <!-- Footer -->
            <StackPanel Grid.Row="3" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,20,0,0">
                <Button Content="HỦY" Style="{StaticResource SmartDownloadWindowSecondaryButtonStyle}" Margin="0,0,10,0" Click="Close_Click" />
                <Button x:Name="btnStartDownload" Content="BẮT ĐẦU TẢI" Style="{StaticResource SmartDownloadWindowPrimaryButtonStyle}" Click="StartDownload_Click" />
            </StackPanel>
        </Grid>
    </Border>
</Window>
