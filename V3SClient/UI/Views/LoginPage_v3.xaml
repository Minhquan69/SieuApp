<Page x:Class="V3SClient.UI.Views.LoginPage_v3"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:Icon="http://metro.mahapps.com/winfx/xaml/iconpacks"
      Background="#061628">
    <Page.Resources>
        <Style x:Key="LoginLabel" TargetType="TextBlock"><Setter Property="Foreground" Value="#FFFFFF"/><Setter Property="FontSize" Value="12"/><Setter Property="FontWeight" Value="Bold"/><Setter Property="Margin" Value="0,0,0,8"/></Style>
        <Style x:Key="LoginField" TargetType="TextBox" BasedOn="{StaticResource VmsTextBoxStyle_v3}"><Setter Property="Height" Value="48"/><Setter Property="FontSize" Value="14"/><Setter Property="Background" Value="#B311263D"/><Setter Property="BorderBrush" Value="#496E97"/><Setter Property="Foreground" Value="#F4F8FF"/><Setter Property="Padding" Value="14,0"/><Setter Property="VerticalContentAlignment" Value="Center"/></Style>
        <Style x:Key="LoginButton" TargetType="Button" BasedOn="{StaticResource PrimaryButtonStyle_v3}">
            <Setter Property="Height" Value="50"/><Setter Property="FontSize" Value="14"/><Setter Property="FontWeight" Value="Bold"/><Setter Property="Foreground" Value="White"/><Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Background" Value="#10355D"/>
            <Style.Triggers>
                <DataTrigger Binding="{Binding IsLoginReady}" Value="True"><Setter Property="Background"><Setter.Value><LinearGradientBrush StartPoint="0,0" EndPoint="1,0"><GradientStop Color="#2563EB"/><GradientStop Color="#0EA5E9" Offset="1"/></LinearGradientBrush></Setter.Value></Setter></DataTrigger>
            </Style.Triggers>
        </Style>
        <Style x:Key="ProfileItem" TargetType="ComboBoxItem">
            <Setter Property="Foreground" Value="#EAF2FF"/><Setter Property="Background" Value="Transparent"/><Setter Property="Padding" Value="14,10"/>
            <Setter Property="Template"><Setter.Value><ControlTemplate TargetType="ComboBoxItem"><Border x:Name="ItemRoot" Background="{TemplateBinding Background}" CornerRadius="4" Margin="4,2"><TextBlock Text="{Binding Name}" Foreground="{TemplateBinding Foreground}" Padding="{TemplateBinding Padding}" VerticalAlignment="Center"/></Border><ControlTemplate.Triggers><Trigger Property="IsHighlighted" Value="True"><Setter TargetName="ItemRoot" Property="Background" Value="#1D4164"/></Trigger><Trigger Property="IsSelected" Value="True"><Setter TargetName="ItemRoot" Property="Background" Value="#245A86"/></Trigger></ControlTemplate.Triggers></ControlTemplate></Setter.Value></Setter>
        </Style>
        <Style x:Key="ProfileCombo" TargetType="ComboBox">
            <Setter Property="Foreground" Value="#EAF2FF"/><Setter Property="Background" Value="#B311263D"/><Setter Property="BorderBrush" Value="#496E97"/><Setter Property="BorderThickness" Value="1"/><Setter Property="Height" Value="48"/><Setter Property="Padding" Value="14,0"/>
            <Setter Property="Template"><Setter.Value><ControlTemplate TargetType="ComboBox"><Grid><ToggleButton x:Name="DropDownToggle" Focusable="False" ClickMode="Press" IsChecked="{Binding IsDropDownOpen, Mode=TwoWay, RelativeSource={RelativeSource TemplatedParent}}" Background="{TemplateBinding Background}" BorderBrush="{TemplateBinding BorderBrush}" BorderThickness="{TemplateBinding BorderThickness}"><ToggleButton.Template><ControlTemplate TargetType="ToggleButton"><Border x:Name="ToggleRoot" Background="{TemplateBinding Background}" BorderBrush="{TemplateBinding BorderBrush}" BorderThickness="{TemplateBinding BorderThickness}" CornerRadius="4"><ContentPresenter HorizontalAlignment="Stretch" VerticalAlignment="Stretch"/></Border><ControlTemplate.Triggers><Trigger Property="IsMouseOver" Value="True"><Setter TargetName="ToggleRoot" Property="BorderBrush" Value="#60CAFF"/></Trigger></ControlTemplate.Triggers></ControlTemplate></ToggleButton.Template><Grid><TextBlock Text="{Binding SelectedItem.Name, RelativeSource={RelativeSource AncestorType=ComboBox}}" Foreground="{TemplateBinding Foreground}" Margin="14,0,42,0" VerticalAlignment="Center"/><TextBlock Text="&#xE70D;" FontFamily="Segoe MDL2 Assets" FontSize="14" Foreground="#60CAFF" HorizontalAlignment="Right" VerticalAlignment="Center" Margin="0,0,14,0"/></Grid></ToggleButton><Popup x:Name="PART_Popup" Placement="Bottom" AllowsTransparency="True" PopupAnimation="Slide" IsOpen="{TemplateBinding IsDropDownOpen}"><Border Background="#0F263D" BorderBrush="#4F7FA8" BorderThickness="1" CornerRadius="5" Width="{Binding ActualWidth, RelativeSource={RelativeSource TemplatedParent}}" Margin="0,4,0,0"><ScrollViewer MaxHeight="220" VerticalScrollBarVisibility="Auto" Padding="2"><StackPanel IsItemsHost="True"/></ScrollViewer></Border></Popup></Grid></ControlTemplate></Setter.Value></Setter>
        </Style>
    </Page.Resources>
    <Grid ClipToBounds="True">
        <Image Source="/images/login/login-background.png" Stretch="UniformToFill" HorizontalAlignment="Stretch" VerticalAlignment="Stretch"/>
        <Rectangle IsHitTestVisible="False"><Rectangle.Fill><LinearGradientBrush StartPoint="0,0" EndPoint="1,0"><GradientStop Color="#50061628"/><GradientStop Color="#28061628" Offset="0.52"/><GradientStop Color="#60061628" Offset="1"/></LinearGradientBrush></Rectangle.Fill></Rectangle>
        <Grid x:Name="LoginLayout" Margin="126,40,76,30">
            <Grid.ColumnDefinitions><ColumnDefinition Width="*"/><ColumnDefinition Width="650"/></Grid.ColumnDefinitions>
            <Grid.RowDefinitions><RowDefinition Height="*"/><RowDefinition Height="Auto"/></Grid.RowDefinitions>

            <StackPanel x:Name="MarketingPanel" VerticalAlignment="Top" MaxWidth="680" Margin="18,10,78,0">
                <Image Source="/images/logo/ivista_logo.png" Width="220" Height="74" Stretch="Uniform" HorizontalAlignment="Left"/>
                <TextBlock Text="NỀN TẢNG QUẢN LÝ VIDEO THÔNG MINH" Foreground="White" FontSize="20" FontWeight="ExtraBold" Margin="0,20,0,0"/>
                <TextBlock FontSize="18" FontWeight="SemiBold" Margin="0,10,0,0" Foreground="#E8F0FA">
                    <Run Text="Giám sát tập trung"/><Run Text="  •  " Foreground="#2F80ED"/><Run Text="Phân tích AI"/><Run Text="  •  " Foreground="#2F80ED"/><Run Text="Vận hành an toàn"/>
                </TextBlock>
            </StackPanel>

            <Border x:Name="LoginCard" Grid.Column="1" Width="600" HorizontalAlignment="Right" VerticalAlignment="Center" Background="#F00D243D" BorderBrush="#7457A7D8" BorderThickness="1" CornerRadius="18" Padding="54,42">
                <StackPanel>
                    <Border Width="48" Height="48" CornerRadius="24" Background="#332563EB" HorizontalAlignment="Center"><Icon:PackIconMaterial Kind="ShieldLockOutline" Width="24" Height="24" Foreground="#60CAFF" HorizontalAlignment="Center" VerticalAlignment="Center"/></Border>
                    <TextBlock Text="ĐĂNG NHẬP HỆ THỐNG" Foreground="White" FontSize="26" FontWeight="ExtraBold" HorizontalAlignment="Center" Margin="0,16,0,5"/>
                    <TextBlock Text="Truy cập trung tâm giám sát và điều hành" Foreground="#B7C8DB" FontSize="14" HorizontalAlignment="Center" Margin="0,0,0,30"/>
                    <Border Background="#4D5B1420" BorderBrush="#D95B2430" BorderThickness="1" CornerRadius="8" Padding="10" Margin="0,0,0,14"><Border.Style><Style TargetType="Border"><Setter Property="Visibility" Value="Collapsed"/><Style.Triggers><DataTrigger Binding="{Binding HasError}" Value="True"><Setter Property="Visibility" Value="Visible"/></DataTrigger></Style.Triggers></Style></Border.Style><TextBlock Text="{Binding ErrorMessage}" Foreground="#FFE5E8" TextWrapping="Wrap" FontSize="12"/></Border>
                    <StackPanel><StackPanel.Style><Style TargetType="StackPanel"><Setter Property="Visibility" Value="Visible"/><Style.Triggers><DataTrigger Binding="{Binding IsProfileSelectionVisible}" Value="True"><Setter Property="Visibility" Value="Collapsed"/></DataTrigger></Style.Triggers></Style></StackPanel.Style>
                        <TextBlock Text="TÊN ĐĂNG NHẬP" Style="{StaticResource LoginLabel}"/>
                        <Grid Height="48" Margin="0,0,0,17">
                            <Border Background="#B311263D" BorderBrush="#496E97" BorderThickness="1" CornerRadius="4"/>
                            <Grid>
                                <Grid.ColumnDefinitions><ColumnDefinition Width="48"/><ColumnDefinition Width="*"/></Grid.ColumnDefinitions>
                                <Icon:PackIconMaterial Kind="AccountOutline" Width="21" Height="21" Foreground="#60CAFF" HorizontalAlignment="Center" VerticalAlignment="Center" IsHitTestVisible="False"/>
                                <TextBox x:Name="UsernameInput" Grid.Column="1" Text="{Binding Username, UpdateSourceTrigger=PropertyChanged}" TextChanged="UsernameInput_OnTextChanged" Background="Transparent" BorderThickness="0" Foreground="#F4F8FF" CaretBrush="#F4F8FF" FontSize="14" Padding="0,0,14,0" VerticalContentAlignment="Center"/>
                            </Grid>
                        </Grid>
                        <TextBlock Text="MẬT KHẨU" Style="{StaticResource LoginLabel}"/><PasswordBox x:Name="PasswordInput" Height="48" Padding="14,10" FontSize="14" Background="#B311263D" BorderBrush="#496E97" BorderThickness="1" Foreground="#F4F8FF" PasswordChanged="PasswordInput_OnPasswordChanged" Margin="0,0,0,4"/>
                        <TextBlock Text="Quên mật khẩu? Liên hệ quản trị viên" Foreground="#7DD3FC" FontSize="11" HorizontalAlignment="Right" Margin="0,5,0,19"/>
                        <Button x:Name="LoginSubmitButton" Content="ĐĂNG NHẬP" Click="LoginSubmitButton_Click" IsEnabled="False" Style="{StaticResource LoginButton}"/>
                    </StackPanel>
                    <StackPanel><StackPanel.Style><Style TargetType="StackPanel"><Setter Property="Visibility" Value="Collapsed"/><Style.Triggers><DataTrigger Binding="{Binding IsProfileSelectionVisible}" Value="True"><Setter Property="Visibility" Value="Visible"/></DataTrigger></Style.Triggers></Style></StackPanel.Style>
                        <TextBlock Text="CHỌN PROFILE LÀM VIỆC" Style="{StaticResource LoginLabel}"/>
                        <ComboBox ItemsSource="{Binding Profiles}" SelectedItem="{Binding SelectedProfile}" DisplayMemberPath="Name" Style="{StaticResource ProfileCombo}" ItemContainerStyle="{StaticResource ProfileItem}"/>
                        <Grid Margin="0,20,0,0"><Grid.ColumnDefinitions><ColumnDefinition Width="*"/><ColumnDefinition Width="*"/></Grid.ColumnDefinitions>
                            <Button Content="QUAY LẠI" Command="{Binding BackToLoginCommand}" Style="{StaticResource SecondaryButtonStyle_v3}" Height="50" FontWeight="Bold" Margin="0,0,8,0"/>
                            <Button Grid.Column="1" Content="VÀO HỆ THỐNG" Command="{Binding ContinueCommand}" Style="{StaticResource LoginButton}" Margin="8,0,0,0"/>
                        </Grid>
                    </StackPanel>
                    <Border BorderBrush="#35526F" BorderThickness="0,1,0,0" Margin="0,26,0,0" Padding="0,16,0,0"><StackPanel Orientation="Horizontal" HorizontalAlignment="Center"><Ellipse Width="8" Height="8" Fill="#22C55E" Margin="0,0,7,0" VerticalAlignment="Center"/><TextBlock Text="Máy chủ: Đã kết nối" Foreground="#BFE9CF" FontSize="12"/></StackPanel></Border>
                </StackPanel>
            </Border>
            <Grid Grid.Row="1" Grid.Column="1" Width="{Binding ActualWidth, ElementName=LoginCard}" HorizontalAlignment="{Binding HorizontalAlignment, ElementName=LoginCard}" Margin="0,14,0,0">
                <StackPanel Orientation="Horizontal" HorizontalAlignment="Center" VerticalAlignment="Center">
                    <Icon:PackIconMaterial Kind="CogOutline" Width="16" Height="16" Foreground="#9FB5C9" Margin="0,0,8,0"/>
                    <TextBlock Text="Phiên bản 2.6.0" Foreground="#B7C7D8" FontSize="13" VerticalAlignment="Center"/>
                    <Border Width="1" Height="18" Background="#36506B" Margin="22,0" VerticalAlignment="Center"/>
                    <TextBlock Text="© 2026 iVista Tech" Foreground="#B7C7D8" FontSize="13" VerticalAlignment="Center"/>
                </StackPanel>
            </Grid>
        </Grid>
    </Grid>
</Page>
